export default function AdminPage() {
  return (
    <div style={{ maxWidth: 720, margin: "40px auto", padding: 24 }}>
      <h1>Admin Panel</h1>
      <p>หน้านี้เฉพาะผู้ใช้ที่ role = 'admin'</p>
    </div>
  );
}